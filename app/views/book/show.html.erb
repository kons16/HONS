<dic class="itemposition">
Amazon 書籍ランキング
<% if @booksrank.present? %>
  <% @booksrank.each do |book| %>
    <div class="list">
      <%= link_to (image_tag(book.image_url, style: "width: 70px;height: 100px;display: inline-block;")), book.url %><br />
      <%= book.title %>
    </div>
  <% end %>
<% end %>
  
</div>